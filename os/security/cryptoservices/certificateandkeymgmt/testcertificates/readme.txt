This directory contains all the certificates used in the testing of the security subsystem.

openssl:

This directory contains the scripts used for generating the certificates using openssl, as well as a script for running the ocsp responder.
It also contains a set of generated certificates.

oldCA:

This contains a regenerated CA for the old Root5 certificate used for all of the Software Install and Java Install.
